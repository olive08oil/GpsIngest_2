# プロジェクトルール

更新日: 2026-06-25

このリポジトリは、外部ルールファイル `C:\Users\olive\SynologyDrive\code\Visual Studio 2026\プロジェクトルール_AI併用版.md` に従って運用する。

## このリポジトリでの適用

- Codexを主担当AIとし、Qwen / Continue / Roo Code / Cline などは補助担当として扱う。
- Git操作はソリューションルート `C:\Users\olive\SynologyDrive\code\Visual Studio 2022\Projects\GPSIngest_2` で実行する。
- このソリューションは複数 `.csproj` 構成のため、Obsidian引き継ぎメモは各Visual Studioプロジェクトフォルダ配下にも置く。
- 既存のルート `docs/obsidian/` は、ユーザーが明示的に作成場所として指定したため、全体統括メモとして維持する。
- Codex以外が変更した内容は、採用前に `agent_review_queue.md` に記録し、Codexまたはユーザー確認後に本線へ反映する。
- 中断処理では、対象プロジェクトの `docs/obsidian/` とルート `docs/obsidian/` を更新し、可能ならGitHubへpushする。
- 再開処理では、まずルートと対象プロジェクトのObsidianメモ、`プロジェクトルール.md`、Git状態を確認する。コード変更はしない。

## ソリューション内プロジェクト

- `GPSIngest/GPSIngest.csproj`
- `GpsIngestClinetForm/GpsIngestClinetForm.csproj`
- `SingularForwarderFunction/SingularForwarderFunction.csproj`
- `RemoteDdbProxyFunction/RemoteDdbProxyFunction.csproj`
- `Infra/src/Infra/Infra.csproj`

## 未確認

- `Infra/src_1/Infra/Infra.csproj` はファイルとして存在するが、`GPSIngest_2.sln` には含まれていない。旧コピーの可能性があるため、現時点ではプロジェクト単位メモ作成対象外とする。
